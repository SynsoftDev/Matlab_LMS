// export constants that are used for project-wide configuration

export default {
  testing:    (process.env.NODE_ENV === 'development'),

  // preload images when hovering over units and sidebar
  preload:    false
}
